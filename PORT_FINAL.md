# 端口最终确认

## ✅ 当前端口配置

### PC端前端
**端口**: **5173** (Vite默认端口)
**访问地址**: http://localhost:5173

### 后端服务
**端口**: 5000
**访问地址**: http://localhost:5000

## 📝 变更历史

1. 第一次：3000 → 8080 (因为3000被占用)
2. 第二次：8080 → 5173 (因为8080也被占用)

## 🚀 启动命令

```bash
# 启动后端
cd backend
python src/app.py

# 启动PC端
cd legal-instructor-pc
npm run dev
```

## ⚠️ 如果5173也被占用

```bash
# 使用8081端口
npm run dev:8081

# 使用3001端口
npm run dev:3001

# 或者指定任意端口
npm run dev -- --port 8888
```

## 📋 常用端口

| 端口 | 用途 |
|------|------|
| 5000 | 后端服务 |
| 5173 | PC端前端（默认） |
| 8081 | PC端前端（备选1） |
| 3001 | PC端前端（备选2） |

---

**最终确认：使用 5173 端口！**
